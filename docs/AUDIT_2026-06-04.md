# SUI Move Contract Audit — 2026-06-04

In-session review of all 10 Move contracts in `contracts/sui/sources/`,
triggered by the operator report "withdrawal lower than expected for some
users" on 2026-06-03. Not a substitute for external review (Movebit /
OtterSec / Halborn) — this captures critical + high findings the
in-session pass surfaced + the fixes shipped.

## Severity scale

- **CRITICAL** — direct fund loss or contract bricking, exploitable today
- **HIGH** — fund-at-risk path or invariant violation requiring narrow conditions
- **MEDIUM** — degraded semantics, surprising behavior, future-scaling concern
- **LOW** — code quality, minor info disclosure

## Coverage

| File | Lines | Pass depth |
|---|---|---|
| community_pool_usdc.move | 1342 | full review |
| community_pool.move (SUI-native) | 1852 | spot-check on shared patterns |
| hedge_executor.move | 640 | full review |
| zk_proxy_vault.move | 657 | full review (phase 2) |
| community_pool_timelock.move | 711 | full review (phase 2) |
| rwa_manager.move | 514 | full review (phase 2) |
| payment_router.move | 495 | full review (phase 2) |
| bluefin_bridge.move | 441 | full review (phase 2) |
| zk_hedge_commitment.move | 452 | full review (phase 2) |
| zk_verifier.move | 375 | full review (phase 2) |

## Findings + fixes

### CRITICAL — community_pool_usdc.move share-math ignored off-chain assets

**Status:** fixed in commits `e8942f76` (oracle field + cron attestation)
and `252a4284` (open_hedge ratio rewire).

`calculate_assets_for_shares`, `calculate_shares_for_deposit`, and
`get_nav_per_share` used only `balance::value(&state.balance) +
VIRTUAL_ASSETS` as total assets — ignoring ~$44 of wBTC/wETH/SUI held on
the admin wallet and BlueFin collateral.

Live impact 2026-06-03: balance $0.41 vs true NAV $44.99. A member
withdrawing 3 shares (10% of pool) would receive $0.135 instead of $4.50.
Symmetric over-issuance on deposit.

Fix: added external NAV oracle via dynamic_field (upgrade-safe — no
struct field added). `admin_attest_external_nav(AdminCap, value, clock)`
with 30% per-call change cap. `admin_set_external_nav_required(bool)`
gates strict-mode freshness assert. Math functions use new internal
`total_assets_including_external`. Cron pushes external NAV every tick
via `attestExternalNav()` in sui-community-pool/route.ts.

### CRITICAL — community_pool_usdc.move close_hedge accepted unverified PnL

**Status:** fixed in commit `252a4284`.

Original `close_hedge` took `pnl_usdc + is_profit + funds` as inputs and
joined whatever funds it got into the pool, regardless of consistency.
A compromised AgentCap holder could call with `is_profit=false,
pnl_usdc=collateral, funds=Coin::zero()`, removing the hedge from
active_hedges and decrementing total_hedged_value without returning any
USDC. Silent drain proportional to any open hedge on-chain.

Fix: assert `coin::value(&funds) >= expected_return` where expected =
`is_profit ? collateral + pnl : max(0, collateral - pnl)`. Generous
returns (e.g. funding rewards captured during hedge life) still accepted;
excess goes into pool balance.

### CRITICAL — community_pool.move close_pool_hedge same drain vector

**Status:** fixed in commit (this commit).

Same shape of bug in the SUI-native sibling. The USDC-pool spec flags the USDC
pool as the production target, but if the SUI-native pool is still
hot-callable, the AgentCap drain works identically.

Fix: same funds-verify assert added to `close_pool_hedge`.

### HIGH — community_pool_usdc.move open_hedge ratios broke after external-NAV fix

**Status:** fixed in commit `252a4284`.

Yesterday's `get_total_nav` change to include external_nav cascaded into
`open_hedge`'s reserve-ratio, max-hedge-ratio, and daily-hedge-cap
checks. At $0.41 on-chain vs $44 external, the 20% reserve floor became
$8.89 — unreachable on-chain. Every cron auto-hedge tick would have
reverted with `E_RESERVE_RATIO_BREACHED`.

Fix: new `get_onchain_nav()` = `balance + hedge_state.total_hedged_value`
used by the open_hedge ratio checks (which are about the contract's
controllable USDC, not depositor wealth). Share-pricing math still uses
the full `get_total_nav()`.

### HIGH — community_pool_usdc.move emergency_withdraw FCFS race

**Status:** fixed in commit `252a4284`.

When the pool is paused or circuit-tripped, members can call
`emergency_withdraw`. Original logic capped payout at
`min(fair_share_value, available_balance)`. First member to call could
drain the balance up to their full fair share, leaving later members
with 0. Severity increased after the external-NAV fix because fair share
value now often exceeds available balance.

Fix: pro-rata cap. `amount = min(fair_share, available × shares /
total_shares)`. Every member gets a proportional cut regardless of call
order. Defensive guard on `amount > 0` around the transfer.

### CRITICAL — hedge_executor.move oracle manipulation by AdminCap

**Status:** fixed in commit (this commit). Contract is testnet-only today
per the deploy commit (`c2205326`).

`update_price_feed` accepted any positive price under AdminCap. No
bounded change, no timestamp, no anti-manipulation. Admin could push
arbitrary price (e.g. drop BTC to $1) between user transactions, causing
`close_hedge` to compute massive PnL either direction and drain the
collateral pool. Single-block rug.

Fix: bound per-update change to 50% of prior value. Still permissive
enough for legitimate volatility (BTC's worst day in 2020 was -45%) but
stops the single-block rugpull. Long-term move to Pyth oracle integration.

### HIGH — community_pool.move shares math ignores hedge state

**Status:** NOT FIXED. Flagged for the same upgrade window as
community_pool_usdc.

community_pool.move (SUI-native, deprecated but possibly
still hot-callable) has the same balance-only share math. `get_total_nav`
there only returns `balance::value(&state.balance)` — even less than
the USDC pool's pre-fix version, which at least included
`hedge_state.total_hedged_value`. If SUI native pool has any off-chain
positions, withdrawing members are underpaid.

Recommended fix: same external-NAV oracle pattern applied to USDC pool.
~150 lines. Defer until/unless the SUI-native pool is confirmed to have
real members + off-chain positions; the USDC pool is the live target.

## Findings observed but not patched in this pass

### MEDIUM — fee math overflow above $500M NAV

`collect_management_fee_internal` computes `(nav * fee_bps *
time_elapsed_sec) / (BPS_DENOMINATOR * SECONDS_PER_YEAR)`. At $500M NAV
× 50 bps × 1 day = 2.16e18, within u64 (1.8e19) but tight. A week of
skipped fee collection at $500M = overflow.

Already mitigated by `NAV_SAFETY_CEILING_USDC` halt and the daily
`sui-collect-fees` cron heartbeat. Long-term: u128 math redeploy
(T4-B in the pre-deposit TODO).

### MEDIUM — performance fee can be inflated via attested external NAV

After the 2026-06-03 oracle fix, the admin-attested external NAV feeds
into `get_nav_per_share`, which feeds into `collect_performance_fee_internal`'s
high-water-mark check. An admin who slightly inflates external NAV
attestation would trigger HWM crossings + performance fee accrual.

Mitigations in place: 30% per-call change cap, fees go to treasury (also
admin-controlled), so this is transparency rather than theft. Long-term:
multi-attestor consensus or Pyth-backed valuation oracle.

### MEDIUM — open_hedge / close_hedge race window

The cron calls `open_hedge` then off-chain swaps USDC to wBTC. Between
the on-chain tx and the off-chain settlement, NAV is misreported as
"hedge_state value moved to admin" while no real off-chain asset exists
yet. Window is ~1-5 seconds in normal operation but could be exploited
by a sandwicher in pathological conditions.

Mitigation: cron operates with sub-second cadence on the swap leg; pool
caps individual hedge size; this is acceptable for current volumes.

### LOW — admin_reset_hedge_state emits UsdcPoolPaused event for non-pause action

`admin_reset_hedge_state` and `admin_reset_daily_hedge` both emit
`UsdcPoolPaused { paused: false }` events as "signal state change"
proxies. Indexers and event subscribers parsing pause events will see
false positives. Recommend dedicated event types.

### LOW — emergency_withdraw doesn't decrement member_count

After full withdrawal in `emergency_withdraw`, `member.shares` is set
to 0 but the member row stays in the table and `member_count` is not
decremented. `get_pool_stats` will report stale member count after an
emergency drain. Cosmetic — does not affect correctness.

## Phase 2 — remaining contracts (audited 2026-06-04 later same day)

### CRITICAL — zk_proxy_vault.move execute_withdrawal cross-proxy drain

**Status:** fixed in commit (this commit).

`execute_withdrawal` asserted `pending.owner == sender` but never that
the `PendingWithdrawal` actually belonged to the `ProxyBinding` passed
in. On SUI, both objects are shared and can be passed by any caller. So
an attacker could:

  1. Create their own proxy A, deposit a small amount, request a
     time-locked withdrawal of $X.
  2. Wait the time-lock (24h default).
  3. Call execute_withdrawal with `pending=their_pending(A, X)` and
     `proxy=victim_shared_proxy_B`. The function decrements `state.tvl`
     by X, marks the pending executed, and splits X SUI from B's
     balance to send to `pending.owner` (= attacker).

End result: $X drained directly out of any victim proxy with sufficient
balance. Bound only by the attacker's initial reservation + the
time-lock wait.

Fix: assert `pending.proxy_id == object::id(proxy)`. Single line; binds
each pending to its source proxy.

### CRITICAL — zk_proxy_vault.move cancel paths inflate wrong proxy accounting

**Status:** fixed in commit (this commit).

Same shape in `cancel_withdrawal` and `guardian_cancel_withdrawal`: no
check that the pending refers to the proxy passed in. After cancelling
the pending, the function credits `proxy.deposited_amount += pending.amount`
to whatever proxy was passed — inflating B's bookkeeping while A's real
balance stays decremented. Sequentially cancellable to manufacture
arbitrary fake credit on any owner's proxy.

Fix: same `pending.proxy_id == object::id(proxy)` assert on both cancel
paths.

### HIGH — community_pool_timelock.move any proposer can cancel any operation

**Status:** fixed in commit (this commit).

`cancel_operation` required only a `ProposerCap`. In a multi-proposer
multisig setup (the entire raison d'être of the timelock), any single
proposer could permanently cancel operations queued by the others —
turning the N-of-N safety into a 1-of-N veto. A rogue or compromised
proposer can paralyse the timelock.

Fix: assert `operation.proposer == ctx.sender()`. Proposers can now only
cancel their own operations. Cross-proposer cancellation must go through
the admin path or by scheduling a counter-operation.

### MEDIUM — community_pool_timelock.move pause didn't halt single-op execution

**Status:** fixed in commit (this commit).

`schedule_operation`, `schedule_batch`, and `execute_batch` all checked
`!paused`. `execute_operation` (the single-op path) did not. So when
admin paused for an active incident, queued ops could still be drained
through the single path.

Fix: added `assert!(!state.paused, E_PAUSED)` at the top of
execute_operation. Now pause is complete.

### HIGH — `verify_zk_proof` is a stub across all ZK paths

**Status:** documented; not fixed in this pass.

Every "ZK proof verification" in the SUI Move side is a length check:

  - `zk_proxy_vault::verify_zk_proof` — returns true if proof ≥64 bytes and
    public_inputs.length ≥ 4 (zk_proxy_vault.move:601-629)
  - `zk_hedge_commitment::verify_settlement_proof` — returns true if
    proof ≥ 64 bytes and `commitment_hash.length > 0`
    (zk_hedge_commitment.move:430-439)
  - `zk_verifier::execute_commitment` — only asserts
    `hash(proof_data).length > 0`, which is always true for non-empty
    input (zk_verifier.move:267-295)
  - `rwa_manager::execute_strategy` — `verified = zk_proof_hash.length > 0`,
    emits event but doesn't gate anything (rwa_manager.move:373-406)

This means the "ZK verification" branding is theatre everywhere on the
SUI side. The real STARK verification happens in the Python prover
(`zkp/api/server.py`); these Move contracts only check that *something*
was submitted. Practical impact varies by call site:

  - In `zk_proxy_vault::withdraw`, the proxy.owner check (line 388) is
    the actual auth gate; ZK proof bypass means "anyone with the proxy
    owner key can withdraw" which is identical to the proxy.owner check
    alone — proof acts as ceremony but adds no security.
  - In `zk_hedge_commitment::settle_commitment`, the RelayerCap gate
    is the real auth; proof has no marginal security.
  - In `zk_verifier::execute_commitment`, the VerifierCap gate is the
    real auth.

Recommended: either implement real on-chain verification (groth16-style
verifier, Pyth-attested proof root, or commitment-to-prover-output
pattern with a centralized prover signing the result) — or remove the
proof input entirely and stop calling these contracts "ZK".

Defer until external audit defines the right pattern.

### MEDIUM — rwa_manager.move portfolio rebalance/strategy are no-ops

**Status:** documented; no fix needed.

`rebalance` updates `last_rebalance` and emits an event but performs no
actual rebalancing. `execute_strategy` emits an event but does no
strategy execution. These are demo stubs. The real RWA accounting must
live elsewhere or be reimplemented. Flagged for product clarity, not a
security issue.

### MEDIUM — payment_router.move sponsor fund semantics

**Status:** documented; no fix needed.

`deposit_sponsor_fund` accepts SUI from anyone. `withdraw_sponsor_fund`
is admin-gated and sends to `fee_recipient` (the admin's beneficiary),
not back to any sponsor. The contract is effectively a donation pot,
not a sponsor reimbursement system as the naming suggests. Either:
  - Rename to make the donation semantics explicit
  - Or change withdraw to a sponsor-specific recipient

Also: `record_sponsored_tx` is accounting-only — it increments
counters but moves no SUI. The contract doesn't actually pay out
sponsored gas; it just tracks intent. Document or wire up payment.

### MEDIUM — bluefin_bridge.move is a pure off-chain mirror

**Status:** documented; no fix needed.

The bridge holds no funds — `BluefinPosition` objects track open/close
state mirrored from BlueFin's off-chain DEX by a RelayerCap. There's no
fund movement path on-chain, so the worst-case attack is a malicious
relayer producing fake positions or sync data, which would corrupt
analytics but cannot move funds. Mitigation lives in the relayer
authorization (RelayerCap distribution), not in the contract logic.

### MEDIUM — bluefin_bridge.move position transferred to trader, not shared

**Status:** documented; no fix needed.

`record_position_open` uses `transfer::transfer(position, trader)` which
makes the position owned by the trader. `record_position_close` and
`sync_position` take `&mut BluefinPosition` — meaning the relayer needs
the trader to wrap the object in their own tx to update it. In practice
this means the relayer cannot sync without trader cooperation, which
defeats the relayer model. Either share the object or accept that sync
will lag.

### LOW — zk_hedge_commitment.move pending_commitments O(n²) drain pattern

**Status:** documented; no fix needed.

`create_batch` repeatedly calls `vector::remove(&mut pending_commitments, 0)`
which is O(n) per call, O(n²) total over `batch_size` elements. At
MAX_BATCH_SIZE=100 this is 10k operations — fine. But at any larger
size, performance degrades sharply. Don't raise MAX_BATCH_SIZE without
refactoring to swap_remove or a queue structure.

## Phase 2 summary

| Severity | Contract | Finding | Status |
|---|---|---|---|
| CRITICAL | zk_proxy_vault | execute_withdrawal cross-proxy drain | fixed |
| CRITICAL | zk_proxy_vault | cancel paths inflate wrong proxy | fixed |
| HIGH | community_pool_timelock | any proposer cancels any op | fixed |
| MEDIUM | community_pool_timelock | execute_operation skipped pause | fixed |
| HIGH | all ZK paths | verify_zk_proof is a stub | documented |
| MEDIUM | rwa_manager | rebalance/strategy are stubs | documented |
| MEDIUM | payment_router | sponsor fund semantics confused | documented |
| MEDIUM | bluefin_bridge | pure off-chain mirror | documented |
| MEDIUM | bluefin_bridge | position transfer breaks relayer sync | documented |
| LOW | zk_hedge_commitment | O(n²) batch drain | documented |

## Phase 3 — remaining "documented" findings fixed (2026-06-04 evening)

### HIGH — ZK is theatre — RESOLVED via opt-in ed25519 prover attestation

**Status:** fixed in commit (this commit). Applied to all 4 affected
contracts: `zk_verifier`, `zk_proxy_vault`, `zk_hedge_commitment`,
`rwa_manager`.

Real on-chain STARK verification in Move is not practical today (no
pairing primitives, gas would dwarf any realistic trade size). The
realistic upgrade path is to make the off-chain STARK prover an
ed25519-signed attestor — equivalent in trust model to Pyth, but
cryptographically anchored on-chain instead of being a length check.

Pattern applied to all four contracts:

  1. New entry function `admin_set_prover_pubkey(AdminCap, state,
     pubkey: vector<u8>)`. Pubkey must be exactly 32 bytes (ed25519
     public key). Empty vector clears the field for emergency
     reversion to insecure mode.
  2. Storage uses `sui::dynamic_field` on the state object's UID with
     a distinct key per contract (`zkv_prover_pubkey_v1`,
     `zpv_prover_pubkey_v1`, `zhc_prover_pubkey_v1`,
     `rwa_prover_pubkey_v1`). This avoids any struct migration in
     already-deployed package state.
  3. Verification functions now check `df::exists_(state.id, KEY)`.
     If a pubkey is set, the first 64 bytes of the submitted proof
     must be a valid `sui::ed25519::ed25519_verify` signature over a
     context-appropriate message:
       - `zk_verifier::verify_proof[_for_portfolio]`: signs the
         `commitment_hash`
       - `zk_verifier::execute_commitment`: signs the
         `commitment.commitment_hash`
       - `zk_proxy_vault::verify_zk_proof`: signs the derived
         `binding_hash(owner, proxy_address)`
       - `zk_hedge_commitment::verify_settlement_proof`: signs the
         `commitment_hash`
       - `rwa_manager::execute_strategy`: signs
         `bcs(portfolio_id) || strategy_bytes`
  4. If no pubkey is set, the legacy "non-empty / ≥64 bytes" check is
     preserved. This is the opt-in migration path — operators MUST
     set the pubkey to remove the theatre. The new `has_prover_pubkey`
     view function lets off-chain ops detect insecure mode.

This converts the contracts from "ZK proof = byte length check" to
"ZK proof = cryptographic attestation from a known prover key" the
moment an operator runs `admin_set_prover_pubkey`. The trust model
becomes equivalent to Pyth: trust the prover key, not the contract
internals.

External audit (T4-C) should validate the message-binding for each
contract and consider whether the signed context covers enough state
to prevent replay across contracts or sessions.

### MEDIUM — payment_router sponsor fund semantics — RESOLVED

**Status:** fixed in commit (this commit).

Two changes:

  1. `withdraw_sponsor_fund` now takes an explicit `recipient: address`
     argument. Previously the destination was hardcoded to
     `state.fee_recipient`, which silently funnelled "sponsor fund"
     deposits to the admin's beneficiary regardless of intent. Admin
     now has to declare the destination explicitly per call.
  2. New entry function `pay_sponsor_gas(SponsorCap, state, beneficiary,
     gas_amount, clock)` actually transfers SUI from `state.sponsor_fund`
     to the beneficiary, enforcing the sponsor's daily limit. This is
     the real payment path. `record_sponsored_tx` remains for
     bookkeeping-only flows (e.g. when gas was sponsored via the SUI
     tx-level sponsored-transaction mechanism and only the daily-limit
     accounting needs to be recorded).

### MEDIUM — bluefin_bridge relayer sync impossibility — RESOLVED

**Status:** fixed in commit (this commit).

`record_position_open` now calls `transfer::share_object(position)`
instead of `transfer::transfer(position, trader)`. The relayer can now
take `&mut` on the position from any transaction to call
`sync_position` and `record_position_close` without needing the trader
to wrap the object first. Trader audit trail preserved via the
`position.trader` field.

### MEDIUM — rwa_manager strategy stubs — RESOLVED via real verification

**Status:** the underlying "no real strategy logic" is documentation —
real strategy actions live off-chain in the orchestrator. The audit
concern was that the on-chain `execute_strategy` function emitted a
"verified" event under a fake length check. With phase 3's ed25519
attestation wired into `execute_strategy`, the function now requires a
real prover signature over `portfolio_id || strategy` bytes to emit the
event. Strategy events from this contract are now cryptographically
attested, even though the on-chain action itself remains a no-op.

### LOW — zk_hedge_commitment O(n²) batch drain

**Status:** documented (not patched). Tried an O(n) rewrite and reverted
— at MAX_BATCH_SIZE=100 the cost is ~5000 ops, well within gas, and the
correct O(1)-amortised refactor needs a head-index queue structure that
should be designed alongside the external audit. Added an in-code
warning not to raise MAX_BATCH_SIZE above ~200 without that refactor.

## Phase 3 summary

| Severity | Contract | Finding | Status |
|---|---|---|---|
| HIGH | 4 ZK contracts | verify is length check, not real proof | fixed (ed25519 attestation) |
| MEDIUM | payment_router | sponsor fund destination hardcoded | fixed (explicit recipient) |
| MEDIUM | payment_router | sponsorship was bookkeeping-only | fixed (added pay_sponsor_gas) |
| MEDIUM | bluefin_bridge | position transfer broke relayer sync | fixed (share_object) |
| MEDIUM | rwa_manager | strategy stub had no real gate | fixed (ed25519 attestation) |
| LOW | zk_hedge_commitment | O(n²) batch drain | documented + bounded |

`sui move build` passes clean (exit 0, lint warnings only — no new
errors introduced).

## Phase 4 — re-audit pass (2026-06-04 late evening)

Triggered by the user's "audit again so it's completely bulletproof"
request before deploy. One real bug found + fixed; two operational
notes documented.

### MEDIUM — zk_verifier replay protection bypassable by byte-append — RESOLVED

**Status:** fixed in commit (this commit).

`verify_proof` and `verify_proof_for_portfolio` dedup on
`keccak256(proof_data)`. After phase 3 wired in real ed25519 signature
verification, the signature is over `commitment_hash` only — the proof
bytes after the signature aren't constrained. An attacker who obtains
ONE valid `(sig, commitment_hash)` pair can construct unlimited
`proof_data` variants by appending garbage bytes:

  - Original proof_data = sig
  - Forged variant N: sig || rand_bytes(N)
  - Each variant produces a different keccak hash → passes the
    `used_proofs` check
  - Each variant still verifies against commitment_hash → passes
    verify_with_prover

End result: replay protection is theatrical. The signed message isn't
unique across calls.

This doesn't directly drain funds — `zk_verifier` is a notification
contract, not a settlement contract — but it lets an attacker pollute
the audit trail with fake "verified" events and potentially induce
downstream automation to act on stale attestations.

Fix: when a prover pubkey IS configured, dedup by the 64-byte ed25519
signature itself instead of by keccak hash. Standard ed25519
signatures are deterministic, so the same signature over the same
message is bit-identical and forms a stable dedup key. The keccak hash
is still stored in the `ProofRecord` so off-chain indexers can verify
the proof_data ↔ record link. When NO prover is configured (legacy
length-check mode), dedup falls back to the keccak hash — preserves
existing behavior during migration.

### OPERATIONAL — withdraw now reverts if on-chain balance can't cover fair share

**Status:** by design, documented for operator awareness.

Post-upgrade behavior of `withdraw`:
  1. Computes `amount_to_withdraw = fair_share_of_total_NAV`
     (including the attested external NAV).
  2. Asserts `balance::value(&state.balance) >= amount_to_withdraw`.

Pre-upgrade: balance-only math silently underpaid. Post-upgrade:
withdraws will REVERT when on-chain USDC is insufficient to cover the
member's fair share. Members never get less than fair value, but they
might have to wait or partial-withdraw if NAV is mostly held off-chain
in wBTC/wETH/SUI on the admin wallet or BlueFin collateral.

Implication for the cron: it must proactively repatriate USDC from
off-chain holdings if the on-chain balance falls below some threshold
relative to total_shares. Suggested threshold: maintain on-chain
balance ≥ 30% of total NAV so any member can withdraw up to ~30% of
their position without the cron needing to unwind.

This is the right semantic (no silent loss) but requires operator
attention to liquidity.

### OPERATIONAL — 30% external-NAV change cap can revert during real crashes

**Status:** by design, mitigation belongs in the cron.

`admin_attest_external_nav` rejects updates that move the attested
value by more than 30% from the previous attestation. The cron pushes
every 30 minutes. If a real >30% move happens in <30 min (a flash
crash where wBTC drops 40%), the attestation reverts → oracle stales
→ strict mode (if on) freezes user flow.

Cron mitigation: when computing the next attestation, clamp the
delta to ≤ EXTERNAL_NAV_MAX_CHANGE_BPS - margin (e.g. 29%) and queue
the remainder for the next tick. After 2–3 ticks the on-chain value
ratchets to the true value. Better than reverting.

For pure-loss moves, the contract being slightly behind the true NAV
during a crash means depositors get SLIGHTLY OVER-issued shares for a
few minutes (NAV looks higher than it is). Bounded by the rate-of-
change cap × ticks-to-converge. Acceptable.

## Phase 4 summary

| Severity | Finding | Status |
|---|---|---|
| MEDIUM | zk_verifier replay bypass via byte-append | fixed (dedup by signature) |
| OPERATIONAL | withdraw reverts when balance < fair share | documented |
| OPERATIONAL | 30% attestation cap can revert on crashes | documented |

`sui move build` passes clean. Total of FOUR audit phases now applied
to this bundle:
  - Morning (e8942f76 → 252a4284): community_pool_usdc share-math + drain + race
  - Mid-day (765e364d): community_pool drain + hedge_executor oracle bound
  - Phase 2 (c21a0a00): zk_proxy_vault cross-proxy + timelock veto
  - Phase 3 (6bb98e5b): ed25519 attestation + sponsor fund + relayer sync
  - Phase 4 (this commit): replay bypass via byte-append in zk_verifier

The bundle is now in a state I'd be willing to deploy. External audit
(T4-C) remains the gate before meaningful AUM.

## Phase 5 — pre-deploy deep audit (2026-06-06)

User requested another deep audit pass before deploy. Five real bugs
found, all in community_pool_usdc.move. All fixed in this commit.
`sui move build` passes clean.

### HIGH — admin_reset_hedge_state created a NAV inconsistency window

**Status:** fixed in commit (this commit).

`admin_reset_hedge_state` clears `total_hedged_value` on-chain. The
external_nav attested by the cron is computed NET of total_hedged_value
(cron subtracts the on-chain hedge tracking to avoid double-counting).
So after admin clears hedge state, `get_total_nav()` reads:
  balance + external_nav (net of OLD hedge) + 0 (cleared hedge)
which under-reports total NAV by the cleared hedge amount until the
cron observes the change and re-attests with the new gross-of-hedge
external_nav value.

A deposit or withdraw landing in that window is priced against the
wrong NAV — the symmetric mirror of the original underpayment bug
that triggered this whole audit. Yesterday's 06-05 unauthorised
maintenance window invoked admin_reset_hedge_state 8 times; if the
pool had been unpaused for normal user flow during that window,
members would have been mispriced.

Fix: `admin_reset_hedge_state` now deletes the `EXTERNAL_NAV_TS_KEY`
dynamic field, forcing `is_external_nav_fresh` to return false. When
strict mode is on (`admin_set_external_nav_required(true)`), any
deposit or withdraw reverts with `E_EXTERNAL_NAV_STALE` until the
cron re-attests with hedge-aware math. When strict mode is off, the
math falls back to balance-only for that interim — better than
silent under-reporting.

### HIGH — collect_management_fee_internal u64 overflow at modest scale

**Status:** fixed in commit (this commit).

Original: `let fee = (nav * fee_bps * time_elapsed_sec) / ...`.
All operands and intermediates were u64. Overflow boundary:

  NAV $10M (raw 10e12) × 100 bps × 1 day (86400 sec) = 8.64e19 > u64::MAX

At $1M NAV with weekly fee collection, overflow. At our current $34
NAV the math is fine, but the cron is supposed to scale to $1B+
which would catastrophically overflow.

Move u64 multiplication aborts on overflow, so the symptom would be
"every deposit/withdraw reverts" once NAV crosses the boundary —
not silent corruption — but the pool would be effectively bricked
until a u128 redeploy.

Fix: u128 intermediates in collect_management_fee_internal and
collect_performance_fee_internal. Final cast to u64 is safe because
the fee is always ≤ nav, which is u64.

### MEDIUM — collect_performance_fee_internal u64 overflow at scale

Same shape, same fix. `gain_per_share × member.shares` can overflow
with realistic per-member positions; replaced with u128 intermediates.

### MEDIUM — first admin_attest_external_nav was unbounded

**Status:** fixed in commit (this commit).

The 30% per-call delta cap only fired when `prior > 0`. First
attestation (when no value had been recorded yet) was unbounded —
admin could push arbitrary external_nav. Consequence: inflated
share price via `get_nav_per_share` triggers HWM crossings on every
member, performance fees accrue against the inflated NAV, treasury
collects, real members are diluted.

This is a one-shot attack window. Once any value is recorded, the
30% cap applies. So the attack requires the AdminCap holder to act
BEFORE any legitimate first attestation lands.

Fix: first attestation is now capped at 100x `total_deposited`. Real
yields don't approach 100x; the bound stops the one-shot nuke while
leaving plenty of room for legitimate value growth. For empty pools
(total_deposited=0), first attestation must be 0 — which is the
contract's default state anyway.

### MEDIUM — total_assets_including_external dilutes new depositors after full exit

**Status:** fixed in commit (this commit).

If all members withdraw (or emergency_withdraw) and `total_shares`
goes to 0, but `external_nav` remains in the dynamic_field with a
stale non-zero value, the next deposit dilutes:

  total_assets = 0 (balance) + 100 (stale external_nav) + VIRTUAL
  total_shares = 0 + VIRTUAL
  shares_to_mint = amount × VIRTUAL / (100 + VIRTUAL)
                ≈ amount / 101  (heavily diluted)

The new depositor pays full amount but receives shares worth a
fraction of what they paid for. The phantom external_nav is
unbacked — no member existed to claim those off-chain holdings.

Fix: `total_assets_including_external` now returns balance only
when total_shares == 0. External_nav is treated as 0 in share-math
view functions until there's at least one member. The cron should
also clear external_nav when total_shares drops to 0 (defensive
operational), but this guard protects against ordering races.

### LOW — clock skew defense in collect_management_fee_internal

`time_elapsed_ms = current_time - last_fee_collection` is plain u64
subtraction. Sui Clock is monotonic per epoch in normal operation,
but cross-epoch boundary or RPC inconsistencies could theoretically
report a current_time below last_fee_collection. The subtraction
would abort, blocking all fee-collecting functions (deposit, withdraw,
collect_fees) until the clock catches up.

Fix: skip fee accrual gracefully when `current_time < last_fee_collection`.
Defense in depth.

## Phase 5 summary

| Severity | Finding | Status |
|---|---|---|
| HIGH | admin_reset_hedge_state NAV inconsistency window | fixed (stale TS_KEY) |
| HIGH | collect_management_fee_internal u64 overflow at $10M NAV | fixed (u128 intermediate) |
| MEDIUM | collect_performance_fee_internal u64 overflow at scale | fixed (u128 intermediate) |
| MEDIUM | first admin_attest_external_nav unbounded | fixed (100x deposited cap) |
| MEDIUM | total_assets dilutes new depositors after full exit | fixed (zero-shares guard) |
| LOW | clock skew aborts fee functions | fixed (graceful skip) |

Total audit phases on this bundle: **5**.

  Morning (e8942f76 → 252a4284): community_pool_usdc share-math + drain
  Mid-day (765e364d): community_pool drain + hedge_executor oracle bound
  Phase 2 (c21a0a00): zk_proxy_vault cross-proxy + timelock veto
  Phase 3 (6bb98e5b): ed25519 attestation + sponsor fund + relayer sync
  Phase 4 (b502dddc): replay bypass via byte-append in zk_verifier
  Phase 5 (this commit): admin_reset NAV race + fee overflow + first-attest + zero-share

## Findings noted but not patched

- **hedge_id collision** (LOW): two opens at same ms+pair+collateral
  produce same id. cooldown_ms makes this impossible in normal operation.
- **leverage=0 accepted by open_hedge** (LOW): no min/max validation on
  leverage input. Off-chain ignores 0; bookkeeping inconsistent.
- **set_treasury allows admin to redirect fees** (centralization):
  pending T4-A multisig migration.
- **External_nav attested by admin is centralized** (architecture):
  bounded by 30% delta cap but still admin-trusted. Pyth integration
  would be the long-term fix.
- **Performance fee accrual uses inflated external_nav** (centralization):
  pending T4-A.

## Phase 6 — capability escalation lockdown (2026-06-06 late)

### HIGH — create_admin_cap was a free persistence escalation

**Status:** fixed in commit (this commit).

Original `create_admin_cap` minted a brand-new AdminCap to any
recipient on demand. Compromise of the existing AdminCap = free
ability to mint backup caps to attacker-controlled addresses, making
key rotation insufficient (attacker can pre-mint persistence). Same
shape for `create_rebalancer_cap`. `add_agent` had the same risk for
AgentCap minting, but with smaller blast radius.

Multi-sig migration (T4-A) is the long-term answer for AdminCap
concentration, but until then any unsanctioned cap-minting is a real
threat — yesterday's unexplained 06-05 maintenance window proves that
"admin actions outside operator awareness" is not hypothetical.

Fix:
  - `create_admin_cap` body now always aborts (signature preserved
    for policy=0 compatible upgrade). Post-deploy, the only path to
    delegate admin authority is `transfer::public_transfer` of the
    existing cap — the standard pattern for multi-sig migration.
  - `create_rebalancer_cap` same fix (aborts always).
  - `add_agent` retains functionality (operator legitimately needs to
    add/rotate agents) but now respects a new lockdown flag set via
    `admin_lock_cap_minting`. Once locked, `add_agent` reverts.
  - New `admin_lock_cap_minting(AdminCap, state)` writes a one-way
    dynamic-field marker. Irreversible by design. Should be called
    once the agent set is final.
  - New view: `is_cap_minting_locked(state) -> bool`.

Operational sequence after deploy:
  1. Confirm the agent set (one cron operator) is the only AgentCap
     holder. If you need to add or rotate operators later, do it BEFORE
     calling admin_lock_cap_minting.
  2. After multi-sig migration completes (T4-A), the multi-sig owns
     the sole AdminCap.
  3. Call `admin_lock_cap_minting` to seal AgentCap minting. From
     then on, even the multi-sig can't add new agents — operator
     rotation requires transferring the existing AgentCap.

## Phase 6 summary

| Severity | Finding | Status |
|---|---|---|
| HIGH | create_admin_cap minted persistence | fixed (body aborts) |
| HIGH | create_rebalancer_cap same pattern | fixed (body aborts) |
| MEDIUM | add_agent had no lockdown | fixed (lockdown flag) |

## Total audit chain

**Six phases** of in-session audit on this bundle.

  Morning (e8942f76 → 252a4284): community_pool_usdc share-math
  Mid-day (765e364d): community_pool drain + hedge_executor oracle
  Phase 2 (c21a0a00): zk_proxy_vault cross-proxy + timelock veto
  Phase 3 (6bb98e5b): ed25519 attestation + sponsor + relayer
  Phase 4 (b502dddc): zk_verifier replay bypass via byte-append
  Phase 5 (42d7179f): NAV inconsistency + fee overflow + first-attest + zero-share
  Phase 6 (this commit): cap-minting lockdown

## Findings explicitly NOT in scope for this upgrade

- **community_pool.move (SUI-native pool):** has same fee-overflow
  bug patterns. Currently empty (0 members, 0 balance) so blast
  radius is zero. Should be paused alongside the USDC pool as a
  belt-and-suspenders measure. Same phase 5/6 fixes could be ported
  but it's deprecated.
- **hedge_executor.move:** testnet-only contract. Has its own bugs
  (collateral pool drain via FCFS profit payout, `leveraged_value`
  u64 overflow at $1B+ scale) but not in production use. The phase 1
  oracle-bound fix already shipped covers the worst-case rugpull.
- **Cron-side NAV computation:** if the cron has a bug, the contract
  faithfully prices using bad input. The 30% per-call delta cap and
  the 100x first-attestation cap limit per-tick damage, but
  long-running attestation drift is still possible. Cron-side audit
  is operator's responsibility.

## Phase 8 — final pre-deploy pass (2026-06-07)

User requested one more complete audit before deploying. Found one
real operational-safety gap; all other paths re-verified clean.

### HIGH — admin_reset_hedge_state could fire in unsafe context

**Status:** fixed in commit (this commit).

Phase 7's fix makes `admin_reset_hedge_state` wipe both
EXTERNAL_NAV_KEY (the value) and EXTERNAL_NAV_TS_KEY (the timestamp).
That creates a brief window where share-math reads as balance-only —
the original underpayment behavior — until the next cron tick lands.
Strict mode is the protection: assert_external_nav_fresh_if_required
reverts user flow because the TS key is gone.

**Gap:** if the operator forgets to enable strict mode AND the pool is
unpaused, the window is unprotected. A deposit lands → over-issued
shares. A withdraw lands → underpayment fires.

Yesterday's 06-05 maintenance window invoked admin_reset_hedge_state
8 times while the pool was paused — that was operationally safe because
the pause check stops user flow at the front. But the function had no
guard requiring the safety net.

Fix: add `assert!(state.paused || is_external_nav_required(state),
E_NOT_AUTHORIZED)` at the top of admin_reset_hedge_state. The function
is now only callable in contexts where its consequences are bounded:
either the pool is paused (front-loaded user-flow block) or strict
mode is on (oracle-staleness assertion catches the wipe). Defense in
depth — operator can no longer accidentally fire this in normal flow.

### Re-verified clean (no findings)

Additional checks done in this phase, all clean:

1. **`is_external_nav_fresh` clock-skew defense** — if `now < ts`
   (recorded ts somehow ahead of current clock), function correctly
   returns false. Strict mode treats this as stale → reverts user
   flow. Defensive in the safe direction.

2. **Dynamic-field key uniqueness** — checked all 4 keys
   (EXTERNAL_NAV_KEY, EXTERNAL_NAV_TS_KEY, EXTERNAL_NAV_REQUIRED_KEY,
   CAP_MINTING_LOCKED_KEY). All distinct byte vectors, all reading
   consistent types (u64 or bool).

3. **`admin_reset_daily_hedge`** — resets daily_hedge_total only,
   no NAV impact, no oracle-field touch. Safe without guard.

4. **`set_treasury` and other admin setters** — change pool params
   but don't create inconsistency windows. Risk is the AdminCap
   concentration itself (T4-A multi-sig). No phase-8 patch needed.

5. **First-deposit math after total_shares=0 + zero balance** —
   `total_assets = balance + VIRTUAL` = 1M raw, `total_shares =
   VIRTUAL` = 1M raw, depositor pays 1 USDC gets 1 share. ERC-4626
   1:1 first deposit holds.

6. **Generics (`<T>`)** — pool is statically typed per shared object.
   USDC pool only flows USDC coins. No "wrong coin type" attacks.

7. **No TODOs / HACKs / "dangerous" markers** in the audited
   community_pool_usdc surface. Grep clean.

## Phase 8 summary

| Severity | Finding | Status |
|---|---|---|
| HIGH | admin_reset_hedge_state could fire without safety net | fixed (paused || strict assert) |
| — | 7 other re-verification checks | clean |

## Total audit chain — 8 phases on `community_pool_usdc.move`

```
Morning  (e8942f76 → 252a4284): underpayment + close_hedge + emergency
Mid-day  (765e364d):            community_pool + hedge_executor oracle
Phase 2  (c21a0a00):            zk_proxy_vault + timelock veto
Phase 3  (6bb98e5b):            ed25519 attestation + sponsor + relayer
Phase 4  (b502dddc):            zk_verifier replay bypass
Phase 5  (42d7179f):            NAV inconsistency + fee overflow + first-attest + zero-share
Phase 6  (5dfb56f2):            cap-minting lockdown
Phase 7  (b68727ca):            admin_reset recovery + E2E verification doc
Phase 8  (this commit):         admin_reset safety guard
```

`sui move build` passes clean. **The contract has been examined for
~30 bug classes across 8 audit phases.** Bundle is ready to deploy
when gas tops up to ≥1 SUI.

## Phase 9 — deep adversarial pass for "billions of dollars" scale (2026-06-07)

User's explicit framing: "people would put in billions, lets be ready."
Done another adversarial pass focused on absolute caps, cast safety,
and the deprecated SUI-native pool that's still callable on-chain.

### MEDIUM — external_nav had no absolute cap on subsequent attestations

**Status:** fixed in commit (this commit).

Phase 5 added a `100x total_deposited` cap on the FIRST attestation.
Subsequent attestations were only bounded by the 30% per-tick delta.
Over many ticks, the cap is exponential: `1.3^N` is 1.3× per tick,
~190× after 20 ticks, ~1.4M× after 50 ticks. At 30-min cron cadence,
a slow-drift attack could 1000x the recorded external_nav in
under 24 hours.

Practical impact: an attacker holding AdminCap (or a buggy cron) could
gradually inflate share price, trigger HWM crossings, harvest
performance fees, until detected. The 30% per-tick is bounded but
the CUMULATIVE damage is not.

Fix: apply the `external_nav_usdc <= 100 × total_deposited` cap to
EVERY attestation, not just the first. Real yields don't approach
100x deposit; this stops the slow-drift attack.

Computation done in u128 to prevent overflow at extreme scale
(`total_deposited × 100` would overflow u64 at $184T+ deposited).

### LOW — defense-in-depth for u128→u64 cast in delta_bps

Move's u128→u64 cast semantics: aborts on overflow in current
versions, but historical Move VMs truncated silently. To be robust
across versions, the bound check on `bps_u128 <= EXTERNAL_NAV_MAX_CHANGE_BPS`
is now done in u128 BEFORE casting, so the cast only happens when
we've already proven the value fits.

### HIGH — community_pool.move (SUI-native) deposit was a buggy backdoor

**Status:** fixed in commit (this commit).

community_pool.move is deprecated but still callable
on-chain. It has the same balance-only share math that caused the
2026-06-03 USDC pool underpayment bug, plus the fee overflow at scale.
Pool is currently empty (0 members, 0 balance), but if any deposit
landed via this pool — accidentally or maliciously — the bugs would
fire from the very first deposit.

Rather than port 8 phases of fixes to a deprecated path, the cleanest
"bulletproof" answer is to abort deposits entirely. Existing members
(currently zero) can still withdraw via the existing withdraw and
emergency_withdraw functions to exit cleanly.

Fix: `deposit` body now transfers the payment back to the caller and
aborts with `E_PAUSED`. Signature preserved for policy=0 compatible
upgrade. The transfer-back is required only for the type checker
(Coin lacks `drop`); the abort rolls back all state anyway.

### Re-verified clean

- **`assert_external_nav_fresh_if_required` coverage** — both deposit
  (line 470) and withdraw (line 546) gate on freshness when strict
  mode is on. emergency_withdraw correctly doesn't gate (pro-rata uses
  raw balance not external_nav); AgentCap-gated open_hedge/close_hedge
  correctly don't gate (cron is the attestation source); collect_fees
  uses internal NAV in a periodic context (stale tolerable).

- **External_nav writers** — admin_attest_external_nav,
  admin_set_external_nav_required, admin_reset_hedge_state are the
  only three. All gated by AdminCap. admin_reset has the phase 7+8
  safety guards.

- **u64 overflow boundaries** — verified safe for the production
  case ($30 NAV). Phase 5 u128 fixes cover the $10M+ scale path.
  Phase 9 absolute cap prevents external_nav from ever exceeding
  100x total_deposited, bounding the attack surface.

## Phase 9 summary

| Severity | Finding | Status |
|---|---|---|
| HIGH | community_pool.move deposit allowed entry to buggy paths | fixed (abort) |
| MEDIUM | external_nav slow-drift attack via 30% per-tick | fixed (absolute cap) |
| LOW | u128→u64 cast safety in delta_bps | fixed (u128 comparison) |

## Audit chain final — 9 phases

```
Morning  (e8942f76 → 252a4284): underpayment + close_hedge + emergency
Mid-day  (765e364d):            community_pool drain + hedge_executor oracle
Phase 2  (c21a0a00):            zk_proxy_vault + timelock veto
Phase 3  (6bb98e5b):            ed25519 + sponsor + relayer
Phase 4  (b502dddc):            replay bypass
Phase 5  (42d7179f):            NAV race + fee overflow + first-attest + zero-share
Phase 6  (5dfb56f2):            cap-minting lockdown
Phase 7  (b68727ca):            admin_reset recovery + E2E doc
Phase 8  (3cc9e57f):            admin_reset safety guard
Phase 9  (this commit):         absolute external_nav cap + community_pool deprecation
```

**~35 bugs found and fixed across 9 phases.** Build clean. Bundle is
ready for deploy when gas tops up to ≥1 SUI.

## "Bulletproof for billions" — honest assessment

What this audit chain HAS achieved:
  - All known fund-loss paths in community_pool_usdc closed
  - External_nav now bounded both per-tick (30%) AND absolute (100×
    deposited) — slow-drift attack blocked
  - Cap-minting lockdown — compromise-recovery requires only key
    rotation, not contract surgery
  - Deprecated paths sealed (community_pool deposit aborts)
  - End-to-end arithmetic verification against live numbers
  - Compatibility verified for the upgrade itself (policy=0)

What remains REQUIRED for actual billion-dollar AUM:
  - **T4-A multi-sig migration** — single AdminCap is the dominant
    residual risk. Compromised admin can still drain via the open
    paths (open_hedge→treasury after set_treasury, attest+collect_fees,
    set_fees, etc.). Multi-sig changes "1 key compromise = drain" to
    "quorum compromise = drain."
  - **T4-B u128 redeploy** — phase 5 u128 fix covers fee math; the
    balance accumulator is still u64. At $500M+ NAV, NAV_SAFETY_CEILING
    halts writes. For true $1B+ a u128 redeploy is required.
  - **T4-C external audit** — Movebit / OtterSec / Halborn. In-session
    audit ≠ adversarial review by people who do this professionally.
  - **Cron correctness** — the cron's NAV computation is the trust
    point. Operator-side audit required for the off-chain code.
  - **Insurance fund / treasury policy** — at billion-dollar AUM,
    operational insurance against tail risks (smart-contract bug
    not caught by audit, validator misbehavior, etc.) becomes
    table stakes. Defer to T5-C.

The contract is as bulletproof as in-session audit can make it. The
remaining items are NOT in-scope for a contract audit — they require
organizational and operational changes.

## Phase 12 — overflow + pause + defense-in-depth pass (2026-06-08)

Yet another fresh-eyes audit pass. Found 4 more issues, all real but
none catastrophic. Build clean, tests 28/28.

### MEDIUM — withdraw circuit-breaker math overflows u64 at $1.8B+ NAV

**Status:** fixed in commit (this commit).

Phase 5 fixed fee math overflow with u128 intermediates. Missed:
withdraw uses `nav × max_single_withdrawal_bps / BPS_DENOMINATOR` and
`nav × daily_withdrawal_cap_bps / BPS_DENOMINATOR` as plain u64. At
nav > u64::MAX / 10000 = 1.8e15 raw = $1.8 billion, the multiplication
aborts. Symptom would be "withdraw reverts at the billion-dollar
threshold." Not silent corruption but absolutely a wall the pool
would hit.

Fix: u128 intermediates, cast to u64 only after divide (where result
is always ≤ nav ≤ u64::MAX).

### MEDIUM — open_hedge reserve/cap math overflows u64 at $1.8-3.6B NAV

**Status:** fixed in commit (this commit).

Same pattern as withdraw: `onchain_nav × bps / BPS_DENOMINATOR` for
the reserve ratio check, max hedge ratio check, and daily hedge cap.
Different ceiling per bps value but all overflow somewhere in the
$1.8-3.6B NAV range. Symptom: open_hedge reverts at scale.

Fix: u128 intermediates throughout.

### MEDIUM — collect_fees missing pause check

**Status:** fixed in commit (this commit).

`FeeManagerCap` holder could call `collect_fees` while pool is paused,
sending accumulated fees to `state.treasury`. The treasury is
admin-controlled. Compromised admin attack chain:
  1. pause the pool (admin)
  2. set_treasury(attacker_address) (admin)
  3. collect_fees(...) (fee_manager)
  → accumulated fees go to attacker, members locked out via pause.

For our pool today this drains pennies — accumulated fees are sub-cent
at $30 NAV. But at $1B+ AUM with 50bps annual fee, accumulated fees
could easily be $137k/day. The attack is real.

Fix: `assert!(!state.paused, E_PAUSED)` at top of collect_fees.
emergency_withdraw remains available for members during pause.

### LOW — is_cap_minting_locked checks df::exists_ only, not value

**Status:** fixed in commit (this commit).

`is_cap_minting_locked` returned true whenever the
CAP_MINTING_LOCKED_KEY dynamic field existed, regardless of its
stored value. Today `admin_lock_cap_minting` only adds the field with
value=true, so this is correct in practice. But if any future code
path added the field with value=false, the existence check would
incorrectly report "locked."

Fix: read the actual u64 → bool value too. Defense in depth.

## Phase 12 summary

| Severity | Finding | Status |
|---|---|---|
| MEDIUM | withdraw circuit breaker overflows at $1.8B NAV | fixed (u128) |
| MEDIUM | open_hedge reserve/cap math overflows at $1.8-3.6B NAV | fixed (u128) |
| MEDIUM | collect_fees no pause check | fixed (pause assert) |
| LOW | is_cap_minting_locked checks exists_ only | fixed (read value) |

## Total audit chain — 12 phases on community_pool_usdc.move

```
Morning  → 252a4284: underpayment + drain + race
Mid-day  → 765e364d: community_pool drain + oracle bound
Phase 2  → c21a0a00: cross-proxy + timelock veto
Phase 3  → 6bb98e5b: ed25519 + sponsor + relayer
Phase 4  → b502dddc: replay bypass
Phase 5  → 42d7179f: NAV race + fee overflow + 4 more
Phase 6  → 5dfb56f2: cap-minting lockdown
Phase 7  → b68727ca: admin_reset recovery
Phase 8  → 3cc9e57f: admin_reset safety guard
Phase 9  → f0da4309: absolute caps + community_pool sealed
Phase 10 → d564db88: diagnose script — 3 bugs
Phase 11 → 9b3d1a9b: tests 28/28 green
Phase 12 → (this):   withdraw/open_hedge overflows + collect_fees pause
```

**~44 bugs found and fixed across 12 phases.** Bundle ready for deploy.

## Fresh-eye verification — all entry functions in community_pool_usdc

For the record, every public entry function has been examined for:
input validation, arithmetic safety, authorization gates, state
invariants, and reachability under pause/circuit-breaker:

  create_pool, deposit, withdraw, emergency_withdraw, collect_fees,
  record_ai_decision, execute_ai_decision, set_allocation, open_hedge,
  close_hedge, admin_set_external_nav_required, admin_attest_external_nav,
  set_paused, trip/reset_circuit_breaker, set_treasury, set_fees,
  set_withdrawal_limits, set_auto_hedge_config, set_rebalance_cooldown,
  set_min_ai_confidence, admin_reset_hedge_state, admin_reset_daily_hedge,
  add_agent, create_admin_cap (DISABLED), create_rebalancer_cap (DISABLED),
  admin_lock_cap_minting

All 26 entry functions, plus the 6+ view functions and the internal
helpers, have been touched in at least one phase. The community_pool_usdc
audit surface is **thoroughly covered** to the depth in-session work
can achieve.

## External review still required

All 10 contracts have been touched at least once by this in-session
pass, but in-session ≠ external. T4-C remains open. Recommend the
external auditor focus on:

1. **The "ZK is theatre" finding (HIGH)** — does the system need real
   on-chain ZK verification, and if so what pattern (groth16 verifier,
   committee-signed prover output, Pyth attestation)?
2. **community_pool_usdc share math under high-frequency external NAV
   attestation** — the 30% per-call change cap was deliberately loose
   for early adoption; auditor should advise on tightening.
3. **AdminCap concentration risk** across all contracts — every contract
   has an AdminCap with broad powers; multisig migration (T4-A) is the
   right answer but the auditor should validate the migration plan.
4. **Reentrancy + flash-loan composability** — none reviewed in this
   pass.

## Recommended next steps before any meaningful AUM

1. **Don't unpause the pool until the audit fixes ship.** The fixes in
   this doc + the original 2026-06-03 oracle fix are all in the same
   upgrade payload — apply them together.

2. **Engage external auditor for the 7 unreviewed contracts.** Budget
   $30-100k, 4-8 weeks (already T4-C in the pre-deposit TODO).

3. **Move `AdminCap` to multi-sig** (already T4-A). Reduces the impact
   of every "compromised AdminCap holder" finding in this audit by
   adding a quorum requirement.

4. **Replace admin-attested oracles with Pyth** wherever they exist.
   Reduces trust surface from "admin can rugpull via stale/false price"
   to "Pyth must remain functional."

5. **Set up Movebit / OtterSec / Halborn engagement** for ongoing review.
