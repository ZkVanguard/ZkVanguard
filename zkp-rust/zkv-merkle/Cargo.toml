[package]
name = "zkv-merkle"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "SHA-256 Merkle tree for the ZkVanguard STARK prover. Byte-identical to Python MerkleTree (zkp/core/cuda_true_stark.py) and Move zkv_merkle."

[lib]
name = "zkv_merkle"
path = "src/lib.rs"

[dependencies]
sha2 = { workspace = true }
