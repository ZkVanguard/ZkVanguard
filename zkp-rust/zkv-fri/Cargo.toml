[package]
name = "zkv-fri"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "FRI folding-consistency verifier for the ZkVanguard STARK. Byte-identical to Python FRI.verify (zkp/core/cuda_true_stark.py) and Move zkv_fri (contracts/sui/sources/zkv_fri.move)."

[lib]
name = "zkv_fri"
path = "src/lib.rs"

[dependencies]
zkv-field = { path = "../zkv-field" }
zkv-merkle = { path = "../zkv-merkle" }
sha2 = { workspace = true }
