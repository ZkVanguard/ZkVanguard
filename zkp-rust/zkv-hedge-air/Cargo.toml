[package]
name = "zkv-hedge-air"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Hedge-invariant composition verifier for the ZkVanguard STARK. Byte-identical to Python hedge_invariant_air / Move zkv_hedge_air."

[lib]
name = "zkv_hedge_air"
path = "src/lib.rs"

[dependencies]
zkv-field = { path = "../zkv-field" }
zkv-merkle = { path = "../zkv-merkle" }
sha2 = { workspace = true }
